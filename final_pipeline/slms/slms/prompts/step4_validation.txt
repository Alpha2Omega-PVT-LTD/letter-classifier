You are an expert clinical validation agent.
Your task is to independently review the validity of an extracted entity.

Entity: {entity}
Category: {category}
Status: {status}
Evidence: {evidence}
Sentence: {sentence}

Answer the following verification questions:
1. Is this entity explicitly written in the clinical text? (Or is it inferred? If inferred, should reject.)
2. Is the entity category correct?
3. Is the status correct?
4. Is there enough evidence in the text for this entity?
5. Should this entity be rejected? (You MUST reject anything inferred. E.g. "Asthma clinic" -> Diagnosis=Asthma -> Reject because it was never explicitly written. "Metformin" -> Diagnosis=Diabetes -> Reject because it is medication).

Respond with ONLY a single valid JSON object. No markdown code fences, no commentary.
The format must be exactly:
{
  "explicitly_written": true,
  "category_correct": true,
  "status_correct": true,
  "enough_evidence": true,
  "should_reject": false,
  "reason": "Explicitly stated in text with direct matching context"
}

JSON Output:
